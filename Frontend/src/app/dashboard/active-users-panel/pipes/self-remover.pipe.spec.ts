import { SelfRemoverPipe } from './self-remover.pipe';

describe('SelfRemoverPipe', () => {
  it('create an instance', () => {
    const pipe = new SelfRemoverPipe();
    expect(pipe).toBeTruthy();
  });
});
